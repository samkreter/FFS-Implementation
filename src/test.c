#include <stdio.h>
#include <stdint.h>
#include <string.h>

int main(){

	// typedef struct hey {
	// 	char test;
	// 	char ha;
	// }hey_t;

	// hey_t boy[20];

	// boy[10].test = 'e';
	// boy[10].ha = 'h';

	// uint8_t buffer[8];

	// memcpy(&buffer,boy+8,8);

	// hey_t temp[4];

	// memcpy(temp,&buffer,8);

	// printf("%c;;",temp[0].test);

	int* test = NULL;
	if(test){
		printf("got it");
	}

	return 0;
}